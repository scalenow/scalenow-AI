<% html_title(t('team_planner.label_team_planner_plural')) -%>

<div class="bg-white p-4 mb-4 rounded-border" style="border: 1px solid #000;">
<%=
  render Primer::OpenProject::PageHeader.new do |header|
    header.with_title(classes: "color-orange") { t('team_planner.label_team_planner_plural') }
    header.with_breadcrumbs([{ href: project_overview_path(@project.id), text: @project.name },
                             t('team_planner.label_team_planner_plural')])
  end
%>

<%=
  render Primer::OpenProject::SubHeader.new do |subheader|
    subheader.with_action_component do
      render ::TeamPlanner::AddButtonComponent.new(current_project: @project)
    end
  end
%>

<%= render ::TeamPlanner::TableComponent.new(rows: @views, current_user: current_user) %>
</div>
